%section.register.container
  .row
    .col-sm-10.col-sm-offset-1
      = bootstrap_form_for(@user, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-6") do |f|
        %header
          %h1 Register
        = f.text_field :full_name
        = f.email_field :email, label: "Email Address"
        = f.password_field :password, help: "Must be 6 characters long."
        = f.password_field :password_confirmation, help: "Must match password."
        = f.submit "Register"