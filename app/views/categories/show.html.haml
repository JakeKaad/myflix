%section.genre
  %header.clearfix
    %h1= @category.name
  %article.row
    - 30.times do
      - video = @category.videos.sample
      .video.col-md-2
        = link_to video_path(video), nil do 
          %img(src="#{video.small_cover_url}")
